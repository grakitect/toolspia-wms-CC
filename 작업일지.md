# 작업일지

작업 내용을 날짜별로 기록합니다. 새 작업은 파일 맨 아래에 이어서 추가합니다.

---

## 2026-07-07

### 바코드 인쇄 페이지 (public/barcode-print.html)

**버그 수정**
- 규격 컬럼 앞에 "0"이 붙어 나오던 오류 (재고값 0 고정 → 표시 로직 수정)
- 라벨 내부 여백 적용 시 바코드/글자가 잘리던 오류 (인쇄물 + 화면 미리보기)
- 미리보기에서 여백이 실제보다 과도하게 적용되던 오류 (축소 비율 미반영)
- 바코드 숫자 크기(%)를 키우면 글자가 잘려 깨지던 오류 (폰트 크기 상한 추가)
- 바코드 숫자 폰트 저해상도(203dpi 프린터) 대응 — 굵은 Arial 계열로 변경, 최소 크기 상향
- 체크박스 컬럼이 상품명과 겹쳐 보이던 오류
- 검색 후 전체선택 클릭 시 검색이 풀리던 오류
- 미리보기와 실제 인쇄물의 바코드 숫자 크기가 서로 다른 공식으로 계산되어 어긋나던 문제 — 인쇄용 계산값(mm)을 미리보기가 그대로 공유하도록 통일

**기능 추가**
- 상품 검색 테이블에 바코드(INN) / 바코드(CT) / 판매상태 / 판매처 컬럼 추가
- 판매처 드롭다운 필터 + 검색창에서 판매처명 검색 지원
- 롤지 라벨 사이즈 프리셋 버튼 (80×50, 100×50, 100×150 기본 등록, 직접 추가/삭제 가능, 브라우저에 저장)

**UI 정리**
- 컬럼 헤더 정비: 바코드→바코드(SKU), SKU→품목코드(이카운트), 재고→규격, 가격 컬럼 삭제
- 상품명/판매상태 등 컬럼 너비 재배분
- "바코드(중포)" 표기를 "바코드(INN)"으로 전체 통일 (public/app.js 포함)
- 인쇄설정/미리보기 레이아웃 비율 2:1로 조정, 미리보기 확대
- 상품 데이터베이스 툴바 정리: 엑셀업로드·양식다운로드·상품추가·CT/INN선택·카테고리선택 버튼 삭제 (판매처 드롭다운으로 대체)

### 기타
- 이마트·롯데마트 재고조사용 엑셀 리스트 생성 (`scripts/export-emart-lotte-stocktake.js`) — 판매처 중복 시 한 행에 함께 표시, 전산재고 포함

---

## 2026-07-10

### 개발 메모 (신규 메뉴)
- 사이드바 최하단에 "개발 메모" 메뉴 신규 추가 — 보드 > 중제목 > 할일 3단계 구조
- 할일을 독립된 카드(칸반 유닛)로 표시, 드래그 앤 드롭으로 같은/다른 중제목·보드 간 이동 및 순서 변경
- 할일 텍스트 자동 줄바꿈(textarea 자동 높이 조절)
- 체크박스·추가(+)/삭제(×) 버튼은 평소엔 숨겼다가 마우스 오버 시에만 표시
- 참고 이미지 기반으로 UI 여러 차례 다듬음 (아이콘 정렬, 버튼 크기/간격, 텍스트 라벨 등)

### 판매처별 코드/상품명 다중화
- 상품 데이터에 `deliveryVendorInfo`(판매처별 { 판매처, 코드, 상품명 }) 배열 도입
  - 기존 단일값 필드(`deliveryVendorCode`/`deliveryItemName`) 제거, 마이그레이션 추가 (판매처가 여러 개면 데이터가 실제로 속한 판매처에 정확히 배정 — 예: 이마트+롯데마트 상품은 기존 값을 롯데마트 항목으로 이관)
- 기본상품정보 / 구매품목리스트 목록을 판매처 수만큼 행으로 펼쳐서 표시, 같은 상품끼리 배경색으로 묶고 "판매처구분"(단일/다중) 컬럼 추가
- 상품 등록/수정 팝업의 판매처 입력을 판매처별 코드/상품명 테이블 방식으로 교체
- 엑셀 업로드(같은 품목코드 행을 그룹으로 묶어 판매처별 정보로 합침)·다운로드(행 펼치기 + 판매처구분 컬럼) 동일하게 반영
- "규격" 컬럼을 "품목명(이카운트)" 바로 옆으로 이동

### 판매처별 출고관리 - 마스터 탭 통일
- 이마트·다이소의 "마스터" 탭을 롯데마트와 동일한 방식으로 통일 — 코드마스터 업로드 대신 기본상품정보 기반 읽기 전용 목록으로 표시, "판매처 상품명" 컬럼 추가
- (참고: 기존 코드마스터 파일 업로드/삭제 UI는 화면에서 제거됨. 데이터/API 자체는 남아있어 출고 파싱 로직엔 영향 없음)

### 데이터 일괄 보정 (일회성 스크립트, `scripts/`)
- `backfill-emart-vendor-names.js`: 그동안 쌓인 이마트 출고 업로드 이력(6762건)에서 상품코드로 매칭해 판매처 코드/상품명 100건 백필
- `update-from-ecount-product-info.js`: `상품정보/이카운트 상품정보.xlsx` 기준으로 품목코드 매칭 후 품목명(상품명+규격 분리)/구분/규격 448건 전수 대조, 177건 수정 (BOM은 파일에 값이 없어 미반영)
- 두 스크립트 모두 적용 전 `data/db.backup-before-*.json` 백업 생성

---

## 2026-07-13

### 기본상품정보 화면 개선 (public/app.js, public/styles.css)
- 판매처 컬럼을 카테고리처럼 색상 태그(칩)로 복원 (`VENDOR_FIXED_COLORS`/`renderVendorChip`) — 판매처별 코드/상품명 다중화 작업 때 빠졌던 것 재적용
- 상품 검색 범위 확대: 품목코드/바코드(SKU·INN·CT)/품목명뿐 아니라 판매처관리코드·판매처 품목명·구매처·구매처 품목코드까지 검색되도록 인덱스 확장
- 품목코드(이카운트) 열을 좌측 고정(sticky) 처리하고, 오른쪽에 스크롤 시에만 나타나는 그림자 효과 추가
  - 시행착오: box-shadow(outset)가 `border-collapse: collapse` 테이블에서 옆 셀에 가려 안 보이는 문제 발견 → `::after` 가상요소로 스크롤 콘텐츠 쪽에 그림자를 얹는 방식으로 해결
  - 최종값: 폭 30px, 시작 투명도 0.12, 스크롤 시에만 표시(`is-scrolled-x` 클래스 토글)
- 컬럼 고정 로직(`applyTableColumnConfig`/`openColumnSettingsUI`)을 1개→N개 고정 컬럼 지원하도록 일반화, 예전에 저장된 컬럼 순서 설정이 있어도 고정 열이 항상 맨 앞에 오도록 버그 수정
- "판매중만 표시" 체크박스 기본값 반전: 기본 상태에서 판매중 상품만 표시, 체크 시 "단종품 포함"으로 전체 표시

### 적재수량/영업담당자 필드 추가 (server.js, public/app.js)
- 신규 컬럼 6개: CTN(EA), INN/CTN, INN(EA), CTN/PLT, PLT(EA), 영업담당자 — 목록/등록폼/엑셀 업로드·다운로드·가이드 전체 반영
- "바코드(CT)" 라벨을 "바코드(CTN)"으로 전체 화면(기본상품정보·구매품목리스트) 통일
- 버그 수정: `upsertProduct`가 필드 화이트리스트 방식이라 새 필드가 저장 시 조용히 삭제되던 문제 발견 → 화이트리스트에 추가, 서버 재시작 후 API 라운드트립으로 검증

### 이마트 판매처 데이터 정리 (scripts/)
- `update-emart-vendor-names-20260713.js`: "이마트 상품정보 판매처상품명 정리-2026-07-13-1.xlsx" 기준 판매처 품목명 206건 매칭, 96건 실제 변경 (매칭 키를 상품코드가 아닌 이카운트코드 우선으로 수정해 미매칭 171건→0건 해결)
- `fill-emart-vendor-code-from-barcode.js`: 이마트 판매처관리코드를 바코드(SKU)와 동일하게 채움 (100건)
- `fill-emart-ct-from-inn.js`: 이마트 판매 상품의 바코드(CT)를 바코드(INN)과 동일하게 채움 (186건)
- 각 스크립트 적용 전 `data/db.backup-before-*.json` 자동 백업

---

## 2026-07-14

### 이마트 확정리스트 유실 복구 및 자동삭제 버그 근본 수정 (server.js)
- 이마트 판매입력 확정리스트가 통째로 사라진 것을 발견 → 백업(`data/db-backups/`)에서 복구 가능한 이마트/롯데마트 확정리스트 전부 복구
- 근본 원인 파악: `outbound-order-upload`의 `apply-batch`/`delete-batches`가 `slipWorkflow`(전 판매처 공용 flat map) 전체 키를 순회하며 지우는 바람에, 한 판매처 파일을 재적용/삭제할 때 다른 판매처의 `slipWorkflow`·`confirmedLists`가 같이 삭제되던 구조적 버그
- 수정: 삭제 대상을 현재 판매처의 slip key로만 한정(`oldPartnerSlipKeys` 캡처), `confirmedLists` 자동 초기화 로직 완전 제거 → 확정리스트는 이제 어떤 업로드/재적용 액션으로도 사라지지 않음
- 실제 장애 사례(여주DRY센터 확정리스트의 판매입력 전송 막힘)를 이 버그로 근본 원인 규명하고 안전하게 패치(재고 중복 반영 없이 `slipWorkflow` 상태만 보정)

### 구매품목리스트 개선 (public/app.js)
- 기본상품정보에 있던 적재수량 컬럼(CTN(EA)/INN·CTN/INN(EA)/CTN·PLT/PLT(EA)) 동일 반영
- "단종품 포함" 토글 추가(기본상품정보와 동일한 패턴)
- 판매처 컬럼에 색상 태그(칩) 적용

### 전체 검색 기능 통합 확장 (public/app.js, server.js)
- 공용 함수 `buildProductSearchText(p)` 신설 — 품목코드/바코드 3종/품목명/판매처코드·상품명/구매처/구매처품목코드/규격까지 한번에 검색
- 마스터·재고·위치재고·조정/이동 폼·바코드 인쇄 검색 등 WMS 내 모든 상품 검색창에 동일 적용
- `/api/history`(입출고 이력) 검색 범위도 동일 수준으로 확장

### 바코드 인쇄 2 신규 구축 (public/barcode-print2.html, 신규 파일)
- BoxHero(boxhero.io/print) 스타일을 참고해 기존 별도 프로그램이던 바코드 인쇄를 WMS 통합형으로 재설계 — 커스텀 라벨 편집기는 유지
- 용지설정(A4라벨용지/롤라벨용지/사용자정의) → 라벨 디자인(기본/커스텀 템플릿 캐러셀) → 상품검색(WMS 상품 담기) → 데이터입력(미등록 상품 수동 입력) 4단계 구성, 미리보기 패널 1000px 고정
- 인쇄/PDF 저장 버튼을 미리보기 타이틀 라인으로 배치, "+커스텀 템플릿 추가"는 점선 버튼 스타일로 라벨 템플릿 페이지와 연결
- 커스텀 템플릿을 서버(db.json, `barcodeCustomTemplates`)에 저장해 바코드 인쇄 1/2가 공유하도록 변경
- 폼텍(Formtec) 라벨지 프리셋 데이터 보강 — 기존 15종 + 신규(비충돌) 27종 추가
- 버그 수정: A4 시트 모드 미리보기/실제 인쇄 크기 불일치(마진 비대칭 케이스), "기본 바코드" 템플릿에서 상품명 텍스트 하단 잘림
- 이마트 판매입력 확정리스트에 "확정 시간" 컬럼 추가

### "자동/무성 삭제" 버그 클래스 재감사 및 수정
- 사용자 피드백으로 범위 재정의: UI 삭제 버튼의 confirm() 여부가 아니라, "실행 버튼을 누르지 않았는데 다른 작업의 부작용으로 자동으로 지워지는 것"이 실제 문제였음(위 확정리스트 버그와 동일 클래스)
- `server.js` 전수 감사로 2건 확정:
  1. **상품 엑셀 업데이트(`/api/products/bulk`) 전체 필드 덮어쓰기**: 일부 컬럼만 있는 업데이트용 시트를 올리면 시트에 없는 필드(안전재고/구매처/카테고리/판매처/비고 등)가 조용히 빈 값으로 초기화되던 문제 → 업로드 시트에 실제로 존재하는 컬럼만 `presentFields`로 표시해 서버에 전달, 없는 필드는 기존 값 유지하도록 `upsertProduct` 수정(단건 등록/수정 폼은 기존처럼 전체 덮어쓰기 유지). 격리 테스트로 3가지 시나리오(부분 업로드/전체 업로드/수동 폼) 모두 검증
  2. **바코드 커스텀 템플릿 동시저장 충돌**: 두 탭/사용자가 동시에 템플릿을 저장하면 전체 배열 덮어쓰기 방식 때문에 한쪽 저장이 다른 쪽이 방금 추가한 템플릿을 지우는 구조 → `/api/barcode-custom-templates`를 항목 단위 `/upsert`, `/delete` API로 교체(barcode-print.html/2.html 동일 반영), 실제 동시 저장 시나리오로 데이터 유실 없음을 확인
- kanban(개발 메모) 카드 삭제, 커스텀 템플릿 삭제 버튼에 `confirm()` 확인창 추가(사용자 요청으로 그대로 유지)

### UI 다듬기
- 롤라벨용지 "자주 쓰는 사이즈" 프리셋을 알약 모양 → 각진 버튼으로 변경, 회색 톤 스타일 적용
- 바코드 인쇄 1/2 새로고침 시 화면이 비어 보이던 버그 수정 — 두 화면은 iframe을 클릭 시에만 지연 생성하는데, 새로고침 후 마지막 화면 복원(`switchView`) 로직에는 그 생성 호출이 빠져 있었음

---

## 2026-07-15

### 기본상품정보 — 다중판매처 표시 개선 (public/app.js, public/styles.css)
- 다중판매처 상품이 판매처마다 여러 줄로 펼쳐져 나오던 방식을 "다중판매처 보기" 토글로 개선: 기본은 "다중 (N)" 배지로 접혀서 한 줄로 표시, 토글 켜면 전체 펼침
- 배지 옆 화살표 칩을 눌러 그 상품 하나만 개별로 펼치기/접기 가능 (전체 토글과 별개로 동작)
- 접힘 상태에서 판매처관리코드/판매처품목명은 "-"로 표시, 판매처 컬럼은 대표 판매처 대신 "다중 (N)" 배지
- 펼침/접힘 화살표 아이콘을 서로 다른 유니코드 문자 대신 하나의 도형을 회전시키는 방식으로 통일 (크기/굵기 불일치 해결)
- 개별로 펼친 다중판매처 그룹에만 왼쪽 파란 컬러바로 시각적 강조 (전체 펼침 상태에서는 표시 안 함)
- 상품정보 테이블 전체에 있던 지그재그(줄무늬) 행 배경 음영 제거

### 반응속도 개선 (public/app.js, public/styles.css)
- 다중판매처 개별 펼치기/접기 시 클릭한 그룹만 갱신하도록 수정 — 이전엔 배지 하나만 눌러도 전체 다중판매처 그룹을 매번 다시 그리던 구조적 비효율 제거
- 목록 필터링 함수(`applyProductListFilters`)를 단일 패스로 재작성하고, 값이 실제로 바뀐 행만 `style.display`를 갱신하도록 수정 (불필요한 스타일 재계산 방지)
- 근본 원인 해결: 상품정보 테이블이 `table-layout: auto`라 셀 하나만 바뀌어도 전체 28개 컬럼 너비를 재계산하던 문제 → `table-layout: fixed`로 전환하고 컬럼별 고정 너비 지정, 넘치는 텍스트는 줄바꿈 처리

### 상품정보 테이블 UI 정리
- 헤더 필터 화살표가 줄바꿈되던 컬럼들 너비 보정, "품목코드(이카운트)" 컬럼은 줄바꿈 안 되는 최소 너비로 축소
- 체크박스가 전역 `input` 스타일(38px 높이)을 그대로 상속받아 비정상적으로 크고 정렬이 어긋나던 버그 수정 — 15px 고정 크기 + 세로 중앙 정렬로 교정, 행 높이도 정상화
- 행을 클릭하면 파란 음영으로 선택되던 기능이 체크박스 클릭에만 반응하던 것을 행 전체 클릭으로 복구

### 정보 가리기(데모 모드) 신규 기능 (public/index.html, public/app.js, public/styles.css)
- 사이드바 맨 아래 "정보 가리기" 토글 버튼 신규 추가
- 클릭 시 현재 화면 및 이후 이동하는 모든 화면의 테이블 데이터를 회사 정보 노출 없이 무작위 값으로 치환 (품목명은 식품류 이름, 판매처/구매처는 가상 회사명, 카테고리·사용창고 등 칩 값도 함께 랜덤화)
- 상태값("판매중" 등)은 그대로 유지, 서버 DB는 전혀 건드리지 않는 순수 화면(DOM)단 처리
- 다시 누르면 실제 데이터로 즉시 복구 — 스레드 포스팅 등 외부 캡처 시 회사 정보 노출 방지 목적

### 기타
- `CLAUDE.md`에 남아있던 옛 소스 위치(`CURSOR_재고관리`) 설명을 실제 작업 저장소(`wms-cc`) 경로로 정정

---

## 2026-07-21

### 정보 가리기(데모 모드) 고도화 (public/demo-mask.js 신규, public/app.js, public/barcode-print.html, public/index.html)
- 사이드바 로고 텍스트("TOOLSPIA WMS")도 켜면 "WMS"로 축약 표시
- 매번 무작위로 바뀌던 방식 → 같은 원본 값은 항상 같은 가짜 값으로 치환되는 해시 기반 결정적 매핑으로 변경, 서로 다른 원본은 겹치지 않게 유일성 보장(충돌 시 다음 후보로 이동)
- 상품명 마스킹 시 그 행의 실제 카테고리(공구/원예/수전/필터/접착/철물/케미/전기 등, 실 데이터 기준 조사)와 어울리는 단어 풀에서 생성 — 식품 위주였던 예전 방식에서 탈피
- 바코드 인쇄1(barcode-print.html) "상품 데이터베이스" 탭에도 동일 마스킹 적용 — 공용 모듈(`demo-mask.js`)로 분리해 메인 화면과 iframe이 같은 매핑 규칙 공유, `localStorage`+`postMessage`로 On/Off 상태 실시간 동기화

### 기본상품정보 — 다중판매처 버그 수정 (public/app.js)
- 새 컬럼 추가 시, 기존에 컬럼순서를 저장해둔 사용자 화면에서 새 컬럼이 앞쪽에 끼어들며 헤더 줄 높이가 늘어나던 문제 → `applyTableColumnConfig`가 저장된 설정에 없는(=새로) 컬럼을 항상 끝에 자동 배치하도록 수정
- 다중판매처 펼치기 시 판매처명·코드·품목명이 깨져 보이던 버그 → 셀을 DOM 위치(`children[n]`)로 찾던 방식이 컬럼 재배치와 충돌 → `data-col-orig-idx` 속성 기반 조회로 교체

### 판매처별 출고 정보 신규 필드 (server.js, public/app.js, public/styles.css)
- 상품마스터에 고정돼 있던 CTN(EA)/INN·CTN/INN(EA)/CTN·PLT/PLT(EA) 5필드는 "입고적재사양"으로 명칭 정리(상품당 1개, 입고 기준)
- 판매처마다 다른 출고 단위/적재사양을 담을 수 있도록 다중판매처 구조에 신규 필드 추가: "출고단위"(EA/INN/CTN/PLT 선택) + "출고적재사양"(입고적재사양과 동일한 5필드, 판매처별로 각각 별도 입력)
- 기본상품정보/구매품목리스트 목록, 상품등록 폼(다중판매처 입력 테이블), 엑셀 내보내기·가져오기 가이드까지 전체 반영
- 목록 화면 헤더에 그룹명(입고적재사양/출고적재사양) 표시 + 구분선 + 정렬 등 세부 스타일 다수 조정(그룹 컬럼 색상 구분, 헤더 줄바꿈, 여백/폭 조정)

### 이카운트 판매입력 비고 항목 추가 (server.js)
- 이마트 등 판매처 확정 → 이카운트 Sale/SaveSale 전송 시 비고(TTL_CTT/U_MEMO2)에 출고일자를 추가로 기재, 상품별 참고란(P_REMARKS1)에 바코드(SKU) 기재
- 출고일자는 업로드 파일명에서 추출한 날짜(예: "0721" → 업로드 당시 연도로 "2026-07-21")를 우선 사용하고, 없으면 기존 방식(입고예정일→확정일자→오늘)으로 대체 — 파일명 날짜가 각 주문 행 → 적용 → 확정 레코드까지 끝까지 전달되도록 연결
- 주의: 이 수정 이전에 이미 확정된 건에는 소급 적용되지 않음(그 레코드 생성 시점 코드에는 해당 필드가 없었음) — 지금부터 새로 업로드/확정하는 건부터 적용

---

## 2026-07-22

### 판매용 복제본 배포
- 동료가 이마트 판매입력 기능만 잠시 쓸 수 있도록 `wms-cc-판매용` 폴더에 스냅샷 복제(node_modules 포함, db.json 현재 데이터 포함) 후 포트 4004로 분리 — 개발 중인 4000번 서버와 충돌 없이 독립 실행

### 이카운트 판매입력 비고 버그 수정 및 형식 변경 (server.js)
- 확정리스트 레코드의 `uploadOrderDateYmd`가 레코드 최초 생성 시점에만 기록되고 이후 재확정 시 갱신되지 않던 버그 수정(필드 도입 이전에 만들어진 레코드도 재확정하면 백필됨)
- 비고 형식을 "센터명 출고 YYYY-MM-DD"로 정리, 센터명에서 "DRY"/"드라이" 표기 제거(너무 길어지는 문제)

### 기본상품정보 화면 — 컬럼 순서 조정 (public/app.js)
- 구매처·영업담당자를 발주담당자 뒤로, 판매처별 출고단위를 판매처구분 뒤로 이동 (`data-col-orig-idx` 기반이라 순서만 재배치, 컬럼 숨김/필터 설정에는 영향 없음)
- 버그 수정: 입고적재사양/출고적재사양 그룹 헤더(`col-group-cell`/`col-group-cell2`)에 걸려있던 `position: relative`가 클래스 선택자라 전역 sticky 헤더 규칙보다 우선순위가 높아, 세로 스크롤 시 이 두 그룹 헤더만 고정이 풀리던 문제 → `position: sticky`로 교체

### 사이드바 메뉴 개편 (public/index.html, public/app.js)
- "기준 정보" 그룹명을 "마스터정보"로 변경
- "기본 정보" 화면에 있던 "거래처 마스터 등록"을 "거래처 정보"라는 별도 메뉴로 분리(`renderPartners`/`#view-partners` 신규)
- "로케이션 관리"·"맵 에디터"·"맵 에디터 3D"를 "로케이션" 서브그룹으로 묶고, "로케이션 관리"는 "관리 / 등록"으로 이름 축약

### 기본 정보 화면 탭 구조로 재구성 (server.js, public/app.js, public/styles.css)
- 회사정보 / 사용자 / 공통 코드 / 창고 4개 탭으로 분리
- **회사정보**: 회사명/사업자번호/주소 신규 저장(`db.companyInfo`) + 이카운트 연동 설정(.env)을 화면에서 직접 수정 가능하도록 신규 API(`GET/POST /api/company-settings`, `saveEnvFile` — 특정 KEY만 갱신하고 즉시 `process.env`에 반영해 재시작 불필요) 추가. 비밀번호/API키는 값을 입력했을 때만 갱신, 화면엔 평문을 내려주지 않고 입력창은 `type="password"`로 마스킹
- **사용자**: 기존 담당자 등록 + 역할 텍스트 태그 추가(`db.managerRoles`, `POST /api/managers/role`) — 로그인/접근권한 제어는 추후 별도 작업으로 미룸
- **공통 코드**: 기존에 있었지만 진입 버튼이 없어 죽어있던 "옵션등록" 모달 대신, 실제 저장된 `productOptions`(카테고리/구분/상태/수급형태/발주부서/발주담당자/영업담당자/창고그룹/판매처명) 값을 탭 화면에서 바로 추가/삭제하도록 신규 구성
- **창고**: 기존 창고 등록 기능 그대로 이동
- 바코드 인쇄 2의 "라벨 디자인" 단계에서 커스텀 템플릿 캐러셀 위에 중복으로 남아있던 "커스텀 템플릿" 라벨 텍스트 제거

---

## 2026-07-23

### 기본 정보 화면 — 기초코드 개편 및 탭 디자인 (public/app.js, public/styles.css)
- "공통 코드" 탭 명칭을 "기초코드"로 변경
- 기본정보 상단 탭 디자인을 참고 이미지에 맞춰 개편 — 활성 탭은 파란색 텍스트 + 텍스트 폭과 동일한 굵은 밑줄(텍스트와 살짝 간격을 둔 일자형 바), 전체 탭 하단엔 얇은 회색 구분선을 별도로 배치(`#master-tab-bar` 스코프 CSS, 다른 화면의 `.ppl-tab-bar`엔 영향 없음)

### 창고 명칭 정리 및 창고그룹(이카운트) 분리 (server.js, public/app.js, data/db.json)
- 실사용 중인 창고(유통사업부/우리물류(회송전)/아세로직스/가온플러스)에 "(W)" 접미사를 붙여 상품 태그 필드인 "창고그룹(이카운트)"와 명확히 구분
- 이력 없던 인터넷사업부/사이툴스/아파치는 창고 목록에서 삭제하고 "창고그룹(이카운트)" 태그로 이동
- 기존 입출고 이력 9,925건, 상품 448건의 `usedWarehouses` 태그를 새 이름으로 일괄 마이그레이션, server.js에 하드코딩돼 있던 기본창고("유통사업부") 관련 fallback 로직도 전부 "유통사업부(W)"로 갱신. 변경 전 db.json은 별도 백업

### 사이드바 "관리" → "설정" 개편 및 사용자 화면 독립 (public/index.html, public/app.js)
- "관리" 그룹명을 "설정"으로 변경
- 기본정보 탭에 있던 "사용자"를 설정 하위의 독립 화면(`renderUsers`/`#view-users`)으로 이동, 기본정보는 회사정보/기초코드/창고 3탭으로 정리

### 알림 화면 실제 구현 (public/app.js)
- 안전재고 이하 상품을 창고/코드/상품명/안전재고/현재고/부족수량으로 나열, "재고현황에서 보기" 클릭 시 재고현황 화면의 재고부족 필터로 이동. 입출고 처리 후 자동 갱신

### 사용자 권한 태그 추가 (server.js, public/app.js)
- 사용자별로 마스터정보/입고/출고/재고/구매/설정/바코드인쇄 7개 권한을 체크박스로 저장(`db.managerPermissions`, `POST /api/managers/permissions`) — 로그인 시스템이 아직 없어 실제 화면 접근 제한은 하지 않고 태그만 저장, 추후 로그인 기능 추가 시 활용 예정

### 옛 "바코드 인쇄" 페이지 삭제 (public/index.html, public/app.js, public/barcode-print.html 삭제)
- 사이드바에서 상품 검색/라벨 템플릿/바코드 인쇄(구버전) 3개 메뉴와 관련 iframe 렌더링 코드 제거, "바코드 인쇄 2 (신규)"만 최상위 메뉴로 남김
- 삭제 전 발견한 문제: 신규 페이지의 "+ 커스텀 템플릿 추가" 버튼이 구버전 페이지의 "라벨 템플릿" 탭으로 이동시켜 템플릿을 만들게 되어 있었음 — 정작 신규 페이지 안에 이미 완전한 템플릿 빌더(`openBuilder`/`builderModal`)가 구현돼 있었는데 새 템플릿 추가 버튼만 엉뚱하게 구버전으로 연결돼 있었던 것(수정 버튼은 원래부터 정상적으로 로컬 빌더를 사용 중이었음). 버튼을 로컬 `openBuilder()` 호출로 교체하고 구버전 이동 함수(`goToLabelTemplatesPage`)를 제거해 신규 페이지가 완전히 독립적으로 동작하도록 수정

### 바코드 인쇄 2 UI 개선 및 이름 정리 (public/barcode-print2.html)
- "바코드 인쇄 2 (신규)" 명칭을 "바코드 인쇄"로 정리(사이드바/페이지 로고/브라우저 탭 제목)
- 1단계 "용지 설정" 좌측 입력값(2/3 너비) + 우측 "시작 칸 선택" 그리드(1/3 너비)로 레이아웃 분리, 그리드 위에 "시작 줄"/"시작 칸" 직접 입력란 추가(그리드 클릭과 양방향 동기화), A4 부분사용 라벨지의 남은 칸부터 인쇄 가능(빈 칸은 null로 표시해 미리보기·실제 인쇄 양쪽에 반영)
- 폼텍 라벨지 규격 셀렉트를 2/3 너비로 줄이고 옆에 "규격 검색" 입력 추가 — 코드 입력 시 해당 규격 자동 선택
- "상품 검색"/"데이터 입력" 두 카드를 "상품 담기" 하나로 통합(번호 제거), 등록 상품 검색·미등록 상품 직접입력을 아이콘+박스로 명확히 구분하고 하나의 "담은 상품 목록" 공유, 엑셀 추가/양식 다운로드는 미등록 상품 블록 안으로 이동

### 인쇄 내역 / 자주 쓰는 인쇄 신규 (server.js, public/index.html, public/app.js, public/barcode-print2.html)
- 사이드바 "바코드 인쇄"를 그룹으로 재구성: 인쇄 / 미등록 상품 인쇄내역 / 자주 쓰는 인쇄 3개 서브메뉴. 세 화면 모두 barcode-print2.html 하나를 `?tab=print|history|presets` 쿼리로 구분해 재사용(완전히 독립된 iframe 인스턴스, 화면 간 직접 함수 호출 없음)
- `db.barcodePrintHistory`(최근 500건 유지) 신규: "인쇄하기"/"PDF 저장" 클릭마다 담긴 상품·수량·등록여부·용지·템플릿을 서버에 자동 기록(`POST /api/barcode-print-history`). 인쇄내역 화면은 기본적으로 "미등록 상품만" 필터로 봄
- `db.barcodePrintPresets` 신규: 인쇄 화면 상단 "자주 쓰는 인쇄"에서 현재 용지/템플릿/담은 상품을 이름+판매처로 저장(`POST /api/barcode-print-presets/upsert`)하고 드롭다운으로 즉시 재적용. "자주 쓰는 인쇄" 관리 화면에서 목록 확인/삭제, "인쇄 화면에서 불러오기" 클릭 시 인쇄 탭으로 전환 후 해당 프리셋을 자동 적용(`?loadPreset=` 쿼리로 전달, 인쇄 탭이 아직 한 번도 안 열린 상태에도 정상 동작하도록 처리)
- 버그 수정: `styles.css`의 `#view-barcode-print2 { height:100% }` 규칙이 새로 추가한 두 섹션(`#view-barcode-print-history`, `#view-barcode-print-presets`)엔 적용되지 않아 iframe이 브라우저 기본 높이(150px)로 쪼그라들던 문제 — 규칙에 두 섹션 추가(이미 삭제된 `#view-barcode-print`는 정리)

### 인쇄 내역 / 자주 쓰는 인쇄 다듬기 (public/barcode-print2.html)
- 서브메뉴명 "미등록 상품 인쇄내역" → "인쇄내역"으로 축약, 순서를 인쇄 → 자주 쓰는 인쇄 → 인쇄내역으로 변경
- "자주 쓰는 인쇄" 목록을 표에서 카드형으로 전환: 왼쪽에 큰 라벨 미리보기, 오른쪽에 이름/판매처뱃지/템플릿·크기·용지·저장일 텍스트, 액션 버튼
- 라벨 미리보기를 인쇄 화면의 실제 미리보기와 동일한 로직(`buildLabelHTML`+`renderBarcodeSVGs`)으로 교체 — 프리셋에 저장된 첫 상품명·바코드가 실제 라벨 비율 그대로 표시됨(바코드형/QR형/커스텀 템플릿 모두 지원), 이후 미리보기 크기 50% 확대
- A4(문서 아이콘)/롤(롤 실루엣 아이콘) 구분 배지 추가 — 처음엔 색 채운 큰 블록으로 만들었다가 "유치해 보인다"는 피드백으로 무채색 얇은 테두리로 톤다운 → 미리보기 오른쪽으로 위치 이동 + 테두리 제거 → 아이콘만 30% 확대
- 용지 설정에서 "사용자정의" 옵션 제거 — 내부적으로 롤라벨용지와 완전히 같은 로직(`pm-roll`)이라 프리셋 버튼 노출 여부만 다른 중복 옵션이었음. 기존에 저장된 사용자정의 프리셋은 하위호환으로 계속 정상 표시
- "현재 설정 저장" 텍스트 링크를 버튼 UI로 바꾸고 문구를 "현재 설정 프리셋 등록"으로 변경

## 2026-07-24

### 인쇄내역 "다시 담기"(재인쇄) 기능 추가 (public/barcode-print2.html)
- 인쇄내역 테이블 각 행에 "다시 담기" 버튼 추가 — 클릭 시 해당 상품·수량을 인쇄 탭의 "담은 상품 목록"으로 즉시 옮기고 인쇄 화면으로 이동. 파손/재고보충으로 특정 상품만 재출력할 때 수량을 다시 입력할 필요 없음
- 버그 수정: `renderBarcodePrint2()` 등이 iframe을 최초 1회만 생성하고 재사용하는 lazy-once 구조라, 인쇄 탭을 이미 한 번 열어본 상태에서는 "다시 담기"가 반영되지 않는 문제 발견 → localStorage 전달 방식에 더해 이미 로드된 iframe을 직접 찾아 함수를 호출하는 폴백을 추가해 두 경우 모두 정상 동작하도록 수정, 브라우저 실측으로 중복 추가 없이 검증 완료

### 라벨 디자인 "텍스트 스타일" 설정 추가 (public/barcode-print2.html)
- 참고이미지(`참고이미지/바코드 라벨 디자인 설정.png`)를 반영해, 2단계 "라벨 디자인"에 기본 템플릿(타입1~3) 전용 텍스트 스타일 패널 추가 — 상품명 / 설명(부가정보: SKU·가격·SKU+가격 중 선택 표시) / 바코드 텍스트 3개 블록, 각각 크기(pt)·굵기·정렬(왼쪽/가운데/오른쪽) 조절 가능, 상품명·설명은 줄 수(1~3줄, 넘치면 말줄임) 추가 조절
- 커스텀 템플릿(직접 만든 템플릿)은 기존 빌더의 요소별 속성 패널을 그대로 사용하며 이번 변경과 무관 — 이번 기능은 기본 제공 템플릿(타입1 기본 바코드/타입2 상세 정보형/타입3 QR코드형) 전용
- 타입2(상세 정보형)의 기존 SKU+가격 표시줄은 그대로 유지(하위호환), "설명" 컨트롤은 원래 설명 줄이 없던 타입1·타입3에 새로 추가하는 선택 항목
- 값을 저장/불러오기(`자주 쓰는 인쇄` 프리셋)에도 반영되도록 `getPrintParams()`/`saveCurrentAsPreset()`/`applyPreset()`에 `textStyle` 필드 추가, 값이 없는 기존 프리셋은 기본값으로 자동 대체(하위호환)
- 브라우저 실측으로 검증: 정렬/굵기/줄바꿈 실시간 미리보기 반영, 타입1·2·3 전환 시 에러 없음, 프리셋 저장 후 기본값으로 되돌렸다가 다시 불러와 원래 설정(크기22·보통·오른쪽 등)이 정확히 복원됨을 확인, 새로고침한 새 세션에서는 변경 전과 동일한 기본 모양 유지(회귀 없음)

### 텍스트 스타일 패널 다듬기 (public/barcode-print2.html)
- 기존에 "라벨 디자인" 상단 별도 행에 있던 "바코드 형식" 선택창을 "바코드 텍스트" 스타일 블록 안으로 이동·통합(재생성되는 블록 안에서도 선택값이 유지되도록 렌더링 시 기존 select의 현재 값을 읽어 다시 그림)
- 굵기(보통/굵게)·정렬(왼쪽/가운데/오른쪽) 버튼을 텍스트 라벨에서 아이콘으로 변경 — 굵기는 "B" 글자를 굵기 차이로, 정렬은 좌/중/우로 정렬된 가로 막대 SVG 아이콘으로 표시, 툴팁(title)으로 접근성 보완

### 바코드 숫자 크기(%) 제거 → 바코드 높이(mm) 직접 설정으로 교체 (public/barcode-print2.html)
- "바코드 숫자 크기 (%)" 필드(전역 배율 방식, 새로 만든 "바코드 텍스트 크기(pt)"와 기능이 겹치던 레거시 컨트롤) 제거, 관련 `fontScale` 배관(getPrintParams/saveCurrentAsPreset/applyPreset)도 함께 정리
- 대신 "바코드 텍스트" 블록에 "바코드 높이 (mm)" 입력을 "바코드 형식" 옆에 추가 — 기존에는 라벨 크기에서 자동 계산(휴리스틱)하던 바코드 막대 높이를 사용자가 직접 지정. 상품명/설명 줄 높이를 먼저 뺀 나머지 공간을 넘지 않도록 자동으로 클램프해 라벨 밖으로 넘치지 않게 처리(기본값 12mm)
- 굵기 아이콘("B") 폰트를 세리프(Georgia)에서 고딕(산세리프, Arial/맑은 고딕)으로 변경
- 브라우저 실측: 높이값 변경 시 바코드 svg 실제 높이 반영 확인, 라벨보다 큰 값(200mm) 입력 시 자동 클램프 확인, 템플릿 1·2·3 전환 및 롤/시트 용지 전환 시 에러 없음 확인

### 텍스트 스타일 블록 이름 정리 및 순서/구성 변경 (public/barcode-print2.html)
- 블록 제목 변경: "설명 (부가정보)" → "부가정보", "바코드 텍스트" → "바코드 스타일"
- "바코드 스타일" 블록의 크기(pt) 라벨을 다른 블록과 구분되게 "텍스트 크기(pt)"로 변경(같은 블록에 바코드 형식·높이도 있어 "무엇의 크기인지" 헷갈리던 문제)
- 상단 별도 행에 있던 "라벨 내부 여백 (mm)"을 "바코드 스타일" 블록 안(바코드 형식·바코드 높이 옆)으로 이동 — 재렌더링돼도 값이 유지되도록 기존 값 보존 처리
- 블록 표시 순서를 바코드 스타일 → 상품명 → 부가정보로 변경(바코드 스타일이 맨 위)
- 브라우저 실측: 블록 순서·라벨 텍스트 확인, 라벨 내부 여백 값이 다른 컨트롤 조작 후에도 유지되는지 확인, 콘솔 에러 없음

### 텍스트 스타일 명칭 재정리 (public/barcode-print2.html)
- 텍스트 스타일 패널 상단의 "텍스트 스타일 (기본 템플릿)" 안내 문구 삭제
- 첫 번째 블록 제목 "바코드 스타일" → "기본 스타일 설정"으로 변경
- 해당 블록의 "텍스트 크기(pt)" 라벨 → "바코드 텍스트 크기(pt)"로 변경
- 브라우저 실측으로 문구 삭제·명칭 변경 확인, 콘솔 에러 없음

### 구매내역(이카운트) 마스터 데이터 1차 임포트 (server.js, scripts/import-purchase-history.js 신규)
- `자료/2024-2026 구매내역 3.xlsx`(이카운트 구매 원장, 2024~2026년 34,686행 + 2번 시트 창고리스트 46행)를 파싱하는 스크립트 작성. 기본은 dry-run으로 요약만 보여주고, `--write`를 붙여야 실제 반영되도록 안전장치
- "품명 및 규격" 컬럼에서 문자열 맨 끝의 대괄호 `[...]`를 규격으로 분리(중간에 있는 대괄호는 품명에 남김). 전체 행의 54.5%(18,893건)에서 규격이 분리됨
- 구매처 165곳을 `db.partners.purchase`에 병합 — 구매처코드가 비어있는 행(1,748건)이 있어 코드가 아닌 이름 기준으로 중복 제거(기존 파트너 upsert 로직과 동일한 기준)
- 신규: `db.purchaseProductMaster`(품목코드 기준 고유 품목 2,879건, 품명/규격/바코드) · `db.ecountWarehouses`(이카운트 창고 리스트 46건 — 실제 재고 추적용 `db.warehouses`와는 별개 마스터). `GET /api/purchase-product-master`, `GET /api/ecount-warehouses` 엔드포인트 추가, `normalizeDb()`에 초기화 코드 추가
- 구매이력(거래 원장 3.4만 건, 약 13MB)은 매 요청마다 통째로 읽고 쓰는 `db.json`에 넣지 않고 `data/purchase-history.json`이라는 별도 파일로 분리 — 전체 앱 성능에 영향 없도록. `GET /api/purchase-history`(vendorName/itemCode/dateFrom/dateTo/limit/offset 쿼리 지원)로 조회
- 반영 전 db.json 백업 후 실행, 브라우저에서 거래처 정보 화면에 구매처 165곳이 정상 표시되는 것까지 확인

### "구매처 정보" 화면 신설 (public/index.html, public/app.js, server.js)
- 사이드바 "구매" 메뉴에 "구매처 정보" 서브메뉴 추가(구매 품목 리스트/구매 발주보다 위, 첫 항목)
- 좌측 구매처 리스트(이름/코드 검색 가능, 품목 수 표시) + 우측 선택한 구매처의 구매 품목 표(품목코드/품목명/규격/누적수량/누적금액/구매횟수/최근구매일), 상단에 품목수·총구매수량·총구매금액·최근구매일 요약
- 신규 서버 엔드포인트 `GET /api/purchase-history/vendor-summary` — `data/purchase-history.json`을 읽어 구매처별·품목별로 서버에서 집계해 응답(165개 구매처, 응답 약 545KB, 1초 이내)
- 검색창에 입력할 때마다 전체 목록을 다시 그리는 구조라 포커스가 끊기는 문제가 있어, 다시 그린 뒤 입력 위치(caret)를 복원하는 처리 추가
- 브라우저 실측: 구매처 목록 클릭 전환, 검색 필터링(연속 타이핑 시 포커스 유지) 정상 확인 — 예를 들어 "㈜신라베딩"은 대괄호 규격 분리 덕분에 품목명이 깔끔하게 "신라-알러지케어 토퍼 소파패드 4인 60x230", 규격이 "그레이"로 잘 나뉘어 표시됨

### 구매처 정보 목록 정렬·탭 추가 (server.js, public/app.js)
- 구매처 목록 정렬 기준을 총구매금액 내림차순 → 거래처명 가나다순으로 변경(`/api/purchase-history/vendor-summary` 서버 응답 정렬)
- 상단에 "전체/수입/국내사입" 탭 추가 — 구매처명에 한글이 하나도 없으면(영문 상호) 수입, 있으면 국내사입으로 자동 분류(사용자 확인: 정확한 회계·통관상 분류가 아니라 이름 기준 간이 분류). 기존 구매 품목 리스트 화면의 `.ppl-tab` 탭 스타일 재사용
- 브라우저 실측: 전체 165 = 수입 67 + 국내사입 98 정확히 일치, 알파벳순 정렬 확인, 탭 전환 시 검색과 함께 정상 필터링

### 버그 수정: 구매처 검색창 한글 입력 시 초성 깨짐 (public/app.js)
- 원인: 검색어 입력(`input` 이벤트)마다 목록 전체(검색창 자신 포함)를 `innerHTML`로 다시 그려서, 한글 조합 중(IME composition)에 `<input>` DOM 노드가 매번 교체돼 조합이 끊기고 초성만 남는 문제
- 목록 렌더링을 분리 — 검색 시에는 `#pv-list`(목록 부분)만 갱신하고 `<input id="pv-search">` 자체는 다시 그리지 않도록 수정. 이전에 있던 임시방편(포커스/커서 위치 강제 복원)도 필요 없어져서 제거
- 브라우저 실측: 검색 후에도 `<input>` DOM 노드가 동일하게 유지되고 포커스가 끊기지 않는 것 확인, 필터링 정상 동작

### 구매처 정보 화면 고도화 — 업체 품목명, 헤더 고정, 구매기록 아코디언 (server.js, public/app.js, public/styles.css)
- "품목명" 헤더를 "품목명(이카운트)"로 변경, 옆에 "업체 품목명" 입력 컬럼 신설 — 이카운트 품목명과 별개로 거래처가 실제 부르는 품목명을 직접 입력해서 저장. 신규 `db.purchaseVendorItemNames`(구매처+품목코드 기준)와 `POST /api/purchase-vendor-item-names`로 저장, `vendor-summary` 응답에 병합해서 내려줌. 입력 중이 아니라 포커스를 벗어날 때(blur)/Enter 시에만 저장해 검색창과 같은 한글 깨짐 문제를 처음부터 피함
- 품목 표 영역에 고정 높이(`calc(100vh - 300px)`)를 주고 자체 스크롤이 생기도록 수정, 헤더가 스크롤에 딸려가지 않도록 고정(`position:sticky`) 처리. 중간에 `<th>`에 sticky를 걸었을 때 안 먹는 문제가 있었는데(크로미움에서 `border-collapse:collapse`인 테이블은 `th` sticky가 무시됨), `border-collapse:separate`로 바꾸고 sticky를 `<thead>` 자체에 거는 방식으로 해결
- 품목 행을 클릭하면 아코디언으로 펼쳐지면서 해당 구매처×품목의 과거 개별 구매기록(날짜/수량/단가/공급가액/합계/창고/인보이스번호/비고)이 최신순으로 나옴 — 기존 `GET /api/purchase-history?vendorName=&itemCode=`를 그대로 재사용, 한 번 불러온 기록은 캐시해서 다시 펼칠 때 재요청 안 함
- 브라우저 실측: 업체 품목명 입력→저장→API 재조회로 반영 확인, 스크롤 시 헤더 고정 확인(수동 sticky 테스트로 원인 진단 후 수정), 품목 클릭 시 아코디언 펼침/접힘과 실제 구매기록(19건 등) 정상 표시 확인

### 버그 수정 + 레이아웃 다듬기: 스크롤 시 행이 헤더 뒤로 비쳐 보이는 문제 (public/app.js, public/styles.css)
- 원인: 품목명처럼 긴 텍스트가 줄바꿈되면서 행 높이가 제각각이 됐고, 스크롤 중 2줄로 줄바꿈된 행의 아랫부분이 고정 헤더 바로 아래에서 반쯤 걸친 채로 보여 "뒤로 지나가는" 것처럼 보였음(사용자 진단대로 배경 자체는 문제 없었고, 진짜 원인은 행 높이 불균일)
- 품목코드/품목명(이카운트)/규격 셀에 한 줄 말줄임(`white-space:nowrap`+ellipsis, 전체 텍스트는 title 툴팁으로) 적용해 행 높이를 균일하게 맞춤. 고정 헤더에도 배경을 thead/tr/th 세 겹으로 중복 지정하고 아래쪽에 옅은 구분선(box-shadow) 추가해 확실히 덮이도록 보강. 아코디언 안의 구매기록 표에도 같은 `.pv-item-table` 스타일 재사용(헤더 고정)
- 추가 요청: 최상단 제목 블록의 설명글을 제목 옆으로 붙이고 블록 패딩을 줄여 차지하는 높이를 축소, 그만큼 아래 목록/품목 표 영역 높이를 `calc(100vh - 300px)` → `calc(100vh - 220px)`로 늘림
- 브라우저 실측: 스크롤해도 행이 헤더 뒤로 깔끔하게 사라짐(겹쳐 보이는 부분 없음), 제목 블록 한 줄로 축소·목록 영역 확장 확인

### 버그 재수정: 여전히 겹쳐 보이던 진짜 원인 — CSS 서술형 선택자가 중첩 테이블까지 잡음 (public/styles.css)
- 사용자가 여전히 겹쳐 보인다고 재확인 요청 → 캡처 대신 실제 좌표(`getBoundingClientRect`)를 찍어보니, 아코디언(구매기록) 안의 중첩 표 헤더가 아코디언 데이터 행들 "중간에" 끼어 렌더링되는 걸 확인
- 진짜 원인: `.pv-item-table thead { position:sticky; ... }`가 후손 결합자(공백)라서, 아코디언 `<td>` 안에 중첩된 `.pv-history-table`의 `<thead>`까지 "바깥 표의 후손"으로 잡혀 같이 sticky가 걸려버림. 지난 턴에 `.pv-history-table`에 별도 스타일을 줬어도 `position`은 안 건드려서(th에만 static을 줬음) thead 자체는 계속 바깥 규칙의 sticky를 물려받고 있었음 — 그래서 겉보기엔 고쳤는데 실제로는 안 고쳐진 상태였음
- `.pv-item-table thead` → `.pv-item-table > thead`(직계 자식 결합자)로 변경해 중첩 표로 새는 것을 원천 차단
- 브라우저 실측: 중첩 표 헤더가 `position:static`으로 정상 확인, 스크롤 중 좌표를 다시 찍어 헤더/데이터 행 순서가 올바르게 나열되는 것 확인, 스크린샷으로도 겹침 없음 최종 확인

### 아코디언(구매기록) 안쪽 표 헤더도 상단 고정 (public/app.js, public/styles.css)
- 위 버그가 CSS 선택자 문제였다는 게 밝혀진 뒤라, 이제 안쪽 표(`.pv-history-table`)에도 안전하게 자체 `position:sticky`를 걸어 아코디언 박스(최대 280px) 안에서 스크롤해도 헤더가 고정되도록 함(바깥 규칙과 스코프가 겹치지 않음을 `element.matches()`로 확인 후 적용)
- 부수적으로 발견: 아코디언 박스에 위쪽 패딩(10px)이 있어서 sticky 헤더와 박스 경계 사이에 10px짜리 틈이 남아 그 틈으로 스크롤 중인 행이 살짝 비치는 게 있었음 — 패딩을 위쪽만 제거해 헤더가 박스 맨 위에 완전히 밀착되게 수정
- 브라우저 실측: 아코디언 안에서 스크롤 시 자체 헤더가 상단에 고정되고 바깥 헤더와도 겹치지 않는 것, 헤더-박스 경계 틈 없음(gap 0px) 확인

### 레이아웃 변경: 왼쪽 목록은 고정(sticky), 오른쪽 품목 표는 박스에서 풀어서 펼침 (public/app.js)
- "리스트 전체가 박스에 갇혀있다"는 피드백 → 절충안으로 왼쪽 구매처 목록은 화면에 붙여두고(sticky), 오른쪽 품목 표는 자체 스크롤박스를 없애 자연스럽게 펼쳐지도록 변경
- 처음엔 바깥 카드의 `height`/`overflow:hidden`을 완전히 없애고 페이지 자체 스크롤에 의존했는데, `.main`이 `overflow-x:hidden`만 줘도 스펙상 sticky의 기준 스크롤 컨테이너로 잡히면서(정작 `.main` 자신은 내용에 맞춰 계속 늘어나 실제로는 절대 스크롤되지 않음) 왼쪽 목록이 sticky가 전혀 안 먹고 그냥 페이지랑 같이 흘러가버리는 문제 발생(`getBoundingClientRect()`로 확인)
- 해결: 제목/탭 아래 목록+품목 표 영역 전체를 감싸는 전용 스크롤 컨테이너(`#pv-scroll-area`, 뷰포트 대부분을 차지하는 높이)를 새로 두어 `.main`의 애매한 스크롤 동작에 기대지 않고 직접 만든 스크롤 기준으로 sticky가 동작하게 함. 오른쪽 품목 표는 이 안에서 별도 박스 없이 자유롭게 늘어나고(더 이상 자체 미니 스크롤바 없음), 왼쪽 목록만 이 영역 상단에 붙어서 계속 보임
- 브라우저 실측: `#pv-scroll-area`를 800px 스크롤해도 왼쪽 목록 박스가 스크롤 영역 맨 위에 정확히 붙어있음(`stickyBoxTop === scrollAreaTop`) 확인, 오른쪽 표는 별도 박스 없이 이어서 스크롤됨, 아코디언 펼침도 정상 동작

### 버그 수정: 품목 클릭 시 구매기록이 가끔 안 나오던 문제 (public/app.js)
- 원인: 행 전체를 클릭 대상으로 두고 "업체 품목명" 입력칸만 `stopPropagation`으로 막아뒀는데, 이 입력칸이 행 너비의 약 18%(실측)를 차지해서 그 위를 클릭하면 토글이 전혀 안 먹었음 — 사용자 입장에선 "가끔 안 눌리는" 것처럼 보였지만 실제로는 클릭 위치에 따라 100% 결정적으로 동작/미동작이 갈리는 문제였음(`.click()`을 스크립트로 반복 실행해 재현·확인)
- 행 전체 클릭 대신, 맨 앞에 전용 화살표 칸(▸/▾)을 추가하고 그 칸만 클릭하면 토글되도록 변경 — 입력칸을 포함해 행의 다른 부분을 클릭해도 토글되지 않지만(의도된 동작), 화살표 칸은 항상 100% 동작함
- 브라우저 실측: 입력칸 클릭 시 토글 안 됨, 화살표 클릭 시 매번 토글됨, 업체 품목명 입력·저장 정상 동작 확인

### 토글 화살표 크게 키움 (public/app.js)
- 텍스트 문자(▸)만 있어 너무 작아 잘 안 보인다는 피드백 → 22×22px 둥근 배경 버튼으로 변경(닫힘: 회색 배경, 펼침: 파란 배경+흰 화살표)으로 눈에 잘 띄게 개선
- 브라우저 실측: 닫힘/펼침 상태에 따라 배경색·화살표 방향이 잘 바뀌고 클릭도 정상 동작

### 구매처명 특수문자(㈜) 정리 — 같은 거래처코드인데 표기가 갈려 따로 나뉘던 항목 병합 (scripts/merge-vendor-name-variants.js 신규)
- 구매처명에 원문자 '㈜'(U+3286)를 쓰는 곳과 일반 텍스트 '(주)'를 쓰는 곳이 같은 이카운트 거래처코드로 섞여 있어, 구매처 정보 화면에서 같은 회사가 두 항목으로 나뉘어 보이던 문제
- `data/purchase-history.json`(구매처 정보는 이 파일의 vendorName 문자열 기준으로 그룹핑됨)과 `db.partners.purchase` 양쪽에서 '㈜' → '(주)'로 일괄 치환하고, 이름이 같아진 항목은 병합(코드가 있는 쪽 우선 유지)하는 스크립트 작성(dry-run 기본, `--write`로 실제 반영)
- 실제로 병합된 항목: (주)루미앤(거래처코드 1358180653, ㈜/⁠(주) 표기 합쳐서 품목 101개), (주)파이네트웍스(1178165372), (주)신라베딩(6728600449, 중복은 없고 표기만 정리). 구매처 총 165 → 163곳으로 정리
- 주의: 같은 코드(1358180653)에 "멀티탑", "(주)지더블유 파트너스" 같은 완전히 다른 회사명도 섞여 있었는데, 이건 ㈜ 표기 문제가 아니라 코드 자체가 여러 회사에 재사용된 것으로 보여(이카운트 쪽 데이터 특성) 이번 작업 범위에서 제외하고 그대로 둠
- 브라우저 실측: 검색창에 "루미앤" 입력 시 "(주)루미앤" 한 항목만 나오고 품목 101개로 합쳐진 것 확인, ㈜ 잔존 항목 0개 확인

---

## 2026-07-28

### 유통사업부 판매현황 분석 및 "판매현황 미등록상품" 탭 신규 구축 (server.js, public/app.js, public/styles.css)
- `유통사업부 판매현황/` 폴더의 2024~2026년 판매 데이터(14개 엑셀 파일) 분석 — 기본상품정보에 등록되지 않은 상품 추출
- 상품정보 화면에 "판매현황 미등록상품" 탭 신규 추가(1회성 임포트, `db.salesUnregisteredProducts`), 기본상품정보와 동일한 컬럼 구성으로 시작
- 이후 요청에 따라 기본상품정보와 완전 동일한 수준으로 기능·서식 전면 확장: 검색, 컬럼 설정, 엑셀 내보내기, 페이지네이션, 단종품 포함/다중판매처 보기 체크박스, 선택 수정/선택 삭제(수정 팝업 포함), 판매처별 다중 펼침/접힘(배경 강조·구분바 포함), 컬럼 너비/순서/식별번호(`data-col-orig-idx`)를 기본상품정보와 바이트 단위로 동일하게 재정렬
- 헤더 색상은 기본상품정보(남색 계열)와 구분되도록 초록 계열 배색으로 별도 적용, 나머지 sticky 첫 열/단종 행 스타일 등은 동일 로직 재사용
- 재렌더링 시 스크롤 위치/검색어/페이지 크기가 초기화되던 문제를 모듈 변수(`salesNewSearchQuery`, `salesNewPageSizeValue` 등) 및 스크롤 좌표 저장/복원으로 해결

### 정보검수 · 판매처별 납품방법/물류대행/상태 컬럼 추가 (server.js, public/app.js, public/styles.css)
- 두 상품 표(기본상품정보/판매현황 미등록상품) 모두에 품목코드 앞 "정보검수" 토글 컬럼 추가 — 클릭 즉시 저장(`infoVerified` 필드, `.verify-toggle` 스위치 UI)
- 판매처별 "납품방법"(택배/용차/화물/직납/위탁), "물류대행"(운송대행/3PL), "판매처별 상태" 드롭다운 컬럼을 `deliveryVendorInfo` 하위 필드로 추가, 인라인 편집 시 자동 저장 — 기존 상품 단위 "상태" 필드/필터/행 음영 처리는 그대로 유지
- `normalizeDb()`에 신규 필드 기본값 초기화 코드 추가

### 엑셀 스타일 컬럼 필터에 검색창 추가 (public/app.js)
- 모든 표에서 공유하는 컬럼 필터 드롭다운(`applyExcelLikeFilter`)에 sticky + 자동 포커스 검색창을 추가해 판매처 등 값이 많은 컬럼에서 검색으로 필터링 가능하도록 개선
- 필터 값 기억(재렌더링 후에도 선택 유지) 로직에 있던 버그도 함께 발견해 수정 — 새로 나타난 값이 기본적으로 "제외됨"으로 취급되던 문제(다중판매처 펼침 시 배경색이 한 개만 보이는 것처럼 보였던 원인)를 공유 함수 레벨에서 근본 수정

### 거래처/판매처 데이터 정합성 정리
- 판매처명 "매장" 관련 잘못된 항목 전체 삭제, 롯데쇼핑 계열 판매처명을 하나의 정식 거래처코드/명칭으로 통합(사용자 확인 거쳐 최종 코드 확정)
- `자료/이카운트 등록 거래처 리스트 전체.xlsx`(2,426건)와 WMS 구매처 목록을 상호명 기준으로 매칭해 구매처코드 불일치 34건을 파일 기준으로 정정, 추가로 파이네트웍스/다올/신성전기산업/동일하이테크 등 표기가 갈린 4개 판매처명 통합
- 구매처코드 누락·중복 사례를 `data/purchase-history.json` 원본 데이터와 대조해 실제 원인(우리 쪽 누락인지 원본 데이터 자체 문제인지) 검증 후 설명
- 미등록상품의 바코드-이카운트코드 중복 17건은 수기 검수용 엑셀(`상품정보/기본상품정보 누락및오류확인/미등록상품_바코드중복_검수용.xlsx`)로 별도 추출 — 사용자 검토 중이라 미등록상품 데이터 수정은 보류 중

### 기본상품정보 "다중판매처" 판정 기준을 이카운트코드 → 바코드(SKU)로 변경 (server.js, scripts)
- 다이소 상품 일부가 플레이스홀더 바코드를 공유하고 있어 순수 바코드 기준 그룹핑을 방해하는 데이터 문제 발견 → 사용자 확인(다이소 전산 기입 오류) 후 해당 84개 상품의 잘못 공유된 `barcode` 필드만 제거(고유한 INN/CTN 바코드는 그대로 유지)
- 이후 "다중판매처"를 이카운트코드 기준이 아닌 실제 바코드(SKU) 기준으로 재정의해 반영 — 이마트/롯데마트 판매입력에 등록된 이카운트코드(`db.outboundCodeMasters`)와 바코드 기준 재대조 결과 기존 연결에 문제 없음을 확인

### 버그 조사 및 임시 조치: 필터 적용 후 보기 개수(페이지 크기) 미적용 (public/app.js)
- "검색 필터 적용 후 보기(50/100/전체) 변경 시 적용 안 됨" 제보 조사 — 여러 재현 시나리오로도 재현하지 못함(원인 미확정, 미해결 상태로 남음)
- 조사 과정에서 별도로 발견한 실제 버그(판매처명 검색 안 되던 문제, `buildProductSearchText`에 판매처명 미포함)는 수정
- 근본 원인을 못 찾은 상태에서 사용자 요청에 따라 두 상품 표의 "보기" 페이지 크기 드롭다운을 "전체"로 고정하고 비활성화하는 임시 조치 적용(추후 원인 파악되면 해제 필요)

### 기타
- Bash `curl -d`로 한글 텍스트를 API에 전달할 때 조용히 깨지는(U+FFFD로 치환) 문제를 발견 — 이미 반영된 값 중 일부(비고, 판매처명 등)가 손상된 것을 확인해 복구, 이후로는 Node 스크립트 파일 방식으로 전환(메모리에 재발 방지 기록)

## 2026-07-29

### 버그 수정: 중복상품 삭제 시 상품 전체가 지워지던 문제 (public/app.js)
- 어제 중복상품 정리 중 롯데마트 상품 2건(바코드 8809599841115, 8809599841085)이 통째로 사라진 것을 백업(`data/db-backups/`)에서 찾아 복원
- 원인 파악: 기본상품정보 표에서 같은 바코드를 쓰는 여러 판매처/상품을 "다중" 그룹으로 묶어 판매처별 한 행씩 보여주는데, 그 행의 체크박스가 "판매처 항목 하나"가 아니라 "상품 전체"에 걸려 있어서, 잘못 들어간 판매처 정보 한 줄만 지우려고 체크 후 "선택 삭제"를 누르면 상품 레코드 전체가 삭제됐음
- 기본상품정보/판매현황 미등록상품 두 표 모두 "선택 삭제"를 개별 판매처 단위로 지울 수 있도록 재설계: 선택된 판매처 항목이 그 상품의 전부면 상품 자체 삭제, 일부만 선택됐으면 해당 판매처 정보만 제거하고 상품은 유지. 확인창에 "상품 N개 완전 삭제 / 판매처 정보 M건만 삭제"로 결과 미리 안내

### 롯데마트 출고 매칭 버그 수정: 판매처 구분 없이 바코드만으로 자동매칭하던 문제 (server.js)
- 같은 바코드를 이마트/롯데마트 상품이 공유하는 구조에서, 롯데마트 출고 주문서 파싱 시 바코드 자동매칭(`buildProductBarcodeMap`)이 `db.products` 배열에서 먼저 나오는 상품(주로 이마트 코드)으로 무조건 매핑해버려서, 구매처 정보 화면에 정확히 등록해둔 롯데마트 전용 매핑(`outboundCodeMasters`)을 무시하는 문제 발견
- `buildProductBarcodeMap`을 판매처 인지형으로 변경 — 해당 판매처로 등록된 상품을 먼저 매칭하고, 못 찾은 바코드만 전체 상품 기준으로 보충하도록 2단계 매칭으로 수정. 실제 데이터로 시뮬레이션해 두 SKU 모두 정확한 롯데마트 코드로 매핑되는 것 확인

### WMS 자체 관리코드(managementCode) 신규 도입 (server.js, public/app.js)
- 이카운트코드/바코드 등 외부 코드만으로는 유일성이 100% 보장되지 않는다는 점(정식 미등록 시 바코드를 코드로 대신 쓰는 경우, 엑셀 업로드 시 실시간 검증 부재 등)을 확인하고, WMS가 상품 생성 시 자체 발급하는 불변 내부 식별자(`WMS-00001` 형식, 기존 `db.seq` 카운터 재사용) 도입
- 기본상품정보(`db.products`)·판매현황 미등록상품(`db.salesUnregisteredProducts`) 기존 데이터 전체(448건 + 716건) 소급 배정, 신규 생성 시 자동 발급, 수정 시에는 값 유지(덮어쓰지 않음)
- 기본상품정보/미등록상품/구매 품목 리스트 세 표 모두에 "관리코드" 컬럼 추가(검색 대상·엑셀 내보내기 포함), 컬럼 폭 미지정으로 눌려 보이던 문제와 예전에 저장된 컬럼 설정(로컬스토리지)이 신규 컬럼을 맨 뒤로 밀어버리던 문제를 저장키 버전업으로 해결

### 상품등록정보 수정 모달 정비 (public/app.js, public/styles.css)
- "선택 수정" 팝업에 그동안 인라인 표 셀렉트로만 고칠 수 있었던 항목(정보검수, 판매처별 상태/납품방법/물류대행) 추가 — 구매 품목 리스트 수정 모달과 공유하는 컴포넌트라 함께 반영됨. 관리코드는 읽기전용으로 표시만
- "입고적재사양"이 맨 아래 구매/발주 정보들 틈에 끼어있어 안 보인다는 피드백에 따라 전체 레이아웃을 기본정보/입고적재사양/판매처정보(출고적재사양 포함)/구매·발주정보/재고설정 섹션으로 재구성, 섹션마다 제목/구분선 추가
- 판매처가 다른데 바코드가 같아 "다중"으로 묶인 상품을 선택 수정할 때, 접힌 대표 행 하나만 체크해도 에러 없이 조용히 엉뚱한 판매처의 상품이 열리던 문제 발견 → 접힌 상태에서는 대표 행 체크박스를 아예 비활성화해 반드시 펼친 뒤 개별 선택하도록 수정(전체선택 체크박스로 우회되는 것도 함께 차단). 서로 다른 상품코드를 동시에 선택했을 때 뜨는 안내 문구도 이유를 알 수 있게 개선

### 데이터 정리
- 다이소 판매처 항목 207건: 출고단위 CTN, 납품방법 직납, 물류대행 운송대행으로 일괄 입력, 판매처별 상태를 상품 상태값과 동일하게 반영

## 2026-07-30

### 기본상품정보 리스트 인라인 편집 개선 (public/app.js, public/styles.css)
- 리스트의 판매처별 상태/납품방법/물류대행 드롭다운(select) 3개를 일반 텍스트로 변경 — 수정은 "선택 수정" 팝업에서만 가능하도록 정리
- 입고적재사양/출고적재사양 5개 항목씩(INN(EA)~PLT(EA))에 더블클릭 시 그 자리에서 바로 숫자 입력 가능한 인라인 편집 추가(Enter 저장/Esc 취소), 저장 시 상품 단위(`saveProductField` 신규)·판매처 단위(기존 `saveVendorField` 재사용) 구분 처리

### 버그 수정: "판매현황 미등록상품" 탭 반복 사용 시 점점 느려지다 결국 멈추는 문제 (public/app.js)
- 최초 제보는 "다중판매처 보기 버튼이 검색 후 동작 안 함"이었으나 재현 결과 실제로는 항상 동작은 하고 있었고, 체감 원인은 성능 문제였음
- 1차로 표 재렌더링 시 셀 값 읽기(`applyExcelLikeFilter`)가 매번 모든 셀에 `querySelector`를 호출하던 부분을 컬럼당 1회 프로브로 최적화하고, 미등록상품 탭의 판매처별 상태/납품방법/물류대행 select 3개도 기본상품정보와 동일하게 텍스트로 변경 — 탭 전환 체감 속도 약 2배 개선(1900행 기준 4.7초 → 1.9~2.3초)했으나 사용자가 "그래도 여전히 느리다"고 재확인
- 추가 조사 끝에 진짜 원인 발견: 엑셀 컬럼 필터의 "바깥 클릭 시 메뉴 닫기" 리스너를 `document`에 등록하면서 중복 등록 방지용으로 `table.dataset.filterOutsideBound` 플래그를 썼는데, 표 자체가 재렌더링마다 통째로 새로 만들어지는 요소라 이 플래그가 항상 초기화되어 매번 새 리스너가 영구적으로 쌓이는 메모리 누수였음(각 리스너가 그 시점 렌더링의 표 전체 DOM을 계속 붙잡고 있어 GC도 안 됨) — 탭 전환/검색/펼치기를 반복할수록 계속 느려지다 결국 응답 없음 상태까지 감
- 모듈 전역에 `activeExcelFilterMenu` 상태와 `closeActiveExcelFilterMenu()`를 두고 `document` 클릭 리스너를 페이지 로드 시 단 1회만 등록하도록 수정. 수정 후 8회 연속 탭 전환 테스트에서 리스너 증가 0건, 전환 속도도 0.66~0.73초로 반복해도 저하 없이 일정하게 유지되는 것 확인
- 페이지네이션(보기 개수 제한)은 여전히 "전체 고정" 상태 — 사용자가 데이터 검증을 위해 전체를 계속 펼쳐봐야 해서 의도적으로 유지, 향후 가상 스크롤 등 별도 개선 필요성 논의는 남겨둠

## 2026-08-03

### 서버 성능 개선 (server.js)
- 매 요청마다 `db.json` 전체를 디스크에서 읽고 파싱하던 `readDb()`에 메모리 캐시 추가 — 파일이 안 바뀐 동안은 디스크 재접근 없이 캐시를 복제해 반환(외부에서 파일이 바뀌면 mtime으로 자동 재로딩)
- 진짜 병목이었던 원인 발견: DB 정규화 함수 `normalizeDb()`(상품·이력 전체를 훑는 O(전체 데이터) 작업)가 캐시 도입 이후에도 **API 요청마다 매번** 재실행되고 있었음 → 디스크 읽기 캐시 시점(콜드 패스)과 쓰기 직전(writeDb)에만 한 번 실행되도록 이동, 매 요청 재요청 제거
- `/api/history`가 상품 448개를 이력 건수(약 1만 건)만큼 `.find()`로 반복 조회하던 O(이력×상품) 이중루프를 코드→상품 Map 조회(O(1))로 교체
- `/api/history`·`/api/recent`가 매번 처음부터 다시 계산하던 "시점재고"(`computeStockAfterEachMovement`) 계산 결과를 데이터가 안 바뀌는 동안 재사용하도록 메모이제이션
- `calculateStock`가 창고 수만큼 이력 전체를 중복 순회하던 것을 한 번의 순회로 재구성
- `/api/history`가 검색조건 없이도 항상 전체 이력(약 4MB)을 응답하던 것을 구분/창고/담당자/날짜 필터를 서버에서 먼저 적용하도록 변경(화면 기본값이 "오늘"이라 대부분의 조회가 크게 빨라짐: 오늘 조회 기준 4MB→1.5KB, 300ms대→130ms대)
- eCvan(이마트 미납 스크래핑) 백그라운드 작업이 API 요청 직렬화 큐(`runSerializedApi`) 밖에서 `db.json`을 직접 읽고 썼던 레이스 컨디션 버그 발견·수정 — `queuedDbWrite()` 헬퍼를 추가해 같은 큐로 합류시킴(스크래핑 도중 다른 요청의 저장을 덮어쓰던 위험 제거)

### 다중판매처 그룹 마스터 편집 기능 신설 (server.js, public/app.js, public/styles.css)
- 같은 바코드를 여러 판매처(이카운트코드가 서로 다름)가 공유하는 "다중판매처" 그룹을 상품정보/판매현황 미등록상품 두 화면 모두에서 마스터 팝업 하나로 한 번에 수정할 수 있도록 재설계 — 기존에는 "1개만 선택하세요"로 막혀있었음
- 팝업에서 관리코드/품목코드(이카운트) 입력칸은 사라지고, 판매처 표에 관리코드(WMS)/품목코드(이카운트)/판매처명 컬럼이 추가되어 그룹에 속한 상품코드들을 한 화면에서 확인·관리. 기본정보/입고적재사양/구매·발주정보는 그룹 전체에 동일하게 저장, 안전/적정재고는 판매처마다 다를 수 있어 그룹수정에서 제외(기존 값 유지)
- 판매처 표에서 "+판매처 추가"로 새 이카운트코드를 입력하면 신규 상품 레코드 생성(상품정보 화면만 해당 — 미등록상품은 신규 생성 API가 없어 기존 코드 수정/삭제만 지원, 버튼 비활성화 처리), 행을 지우고 저장하면 확인창 후 해당 레코드 완전 삭제
- 다중판매처 "펼치기"를 접어둔 상태에서 대표 행 체크박스가 비활성화되던 것을 해제 — 이제 접힌 채로 체크해도 그룹 전체가 바코드 기준으로 올바르게 인식됨
- 팝업 가로폭 900px→1360px로 확대, 판매처 열 너비도 넓혀서 이름이 안 잘리게 조정
- 작업 중 발견한 별도 버그도 같이 수정: 판매처 선택 드롭다운이 미리 정의된 4개(다이소/이마트/롯데마트/홈플러스)만 옵션으로 갖고 있어, 그 외 판매처명(판매이력에서 자동수집된 소규모 유통사명 등)은 저장 시 조용히 빈 값이 되어 사라지는 문제였음 — 다중판매처 그룹 저장에서는 이게 레코드 전체 삭제로도 이어질 수 있어 위험했음. 드롭다운을 검색 가능한 텍스트 입력(datalist, 거래처정보>판매처 마스터 기반)으로 교체해 근본적으로 해결(목록에 없는 값도 그대로 보존됨)
- 실제 다중판매처 그룹 데이터로 회귀 테스트: 공통필드 일괄저장 후 각 레코드의 판매처별 고유 정보(품목명/코드 등)가 안 섞이는지, 안전/적정재고가 그대로 보존되는지, 판매처 추가/삭제가 올바른 레코드에만 반영되는지 확인. 테스트로 만든 임시 데이터는 전부 원상복구
- 거래처정보에 등록된 판매처가 2곳뿐이라 검색 커버리지가 낮다는 점 확인 — 미등록상품 판매처명 정리가 끝나면 거래처정보에 반영하기로 결정(지금 당장 코드 변경 없음)
- 이마트/롯데마트 판매처 항목 253건: 출고단위 INN, 납품방법 직납, 물류대행 운송대행으로 일괄 입력, 판매처별 상태를 상품 상태값과 동일하게 반영

## 2026-08-04

### 조립입고(BOM) 기능 신설 (server.js, public/app.js, public/styles.css)
- 해외/외주 조립처럼 원자재를 들여와 국내에서 완제품으로 조립하는 상품을 위해 원자재-완제품 관계를 BOM으로 등록하는 기능 신설
- 단건입고 화면에서 BOM이 등록된 완제품을 입고하면 자동으로 조립여부를 판별해, 가공처창고의 원자재 재고를 소모한 뒤 완제품을 입고 처리(입고 전 실시간 재고 미리보기 제공)
- 상품이 BOM 대상인지 미리 알 수 있는 사전 체크 플래그와, BOM 전용 경량 편집 모달 추가
- 원자재/부자재 상품에는 어떤 완제품 BOM에 쓰이는지 역참조 뱃지 표시

### 미등록상품 → 기본상품정보 이동 기능, 상품 화면 개편 (server.js, public/app.js, public/styles.css)
- "판매현황 미등록상품" 탭에서 선택한 상품을 기본상품정보로 이동시키는 버튼 추가(미등록상품의 메모 필드는 기본상품정보의 비고 필드로 매핑)
- 기본상품정보/미등록상품 두 화면 공통으로, 이카운트구분 기준 전체/상품류/원자재/부자재 서브탭 신설
- 상단 액션버튼 위치 및 검색행 레이아웃 정리
- 이카운트 구분 라벨 정리, 기본상품정보에 메모 컬럼 추가

## 2026-08-06

### 미등록상품 "롯데패키징솔루션즈(주) 생활용품" 판매처 데이터 일괄 입력 (data/db.json, API 경유)
- 판매처가 "롯데패키징솔루션즈(주) 생활용품"인 미등록상품 55건에 판매처별상태=판매중, 거래유형=내수, 납품방법=직납, 출고단위=INN을 `PUT /api/products/sales-unregistered` API로 일괄 입력(직접 db.json 수정 대신 서버 API 경유로 캐시/백업 로직 그대로 활용)

### 롯데알미늄 발주내역 매칭으로 CTN바코드/판매처관리코드/판매처품목명 채우기 (자료/롯데알미늄 발주내역.xls)
- 위 55건을 `자료/롯데알미늄 발주내역.xls`(457행, SKU 바코드 52종)의 SKU바코드와 상품 바코드 기준으로 매칭 — 50건 매칭 성공, CTN 바코드(logisticsBarcode)/판매처관리코드(vendor code)/판매처 품목명(vendor itemName) 입력
- 5건은 발주내역 파일 자체에 해당 바코드 이력이 없어 매칭 실패로 남김(리스트업: 2020060400003, 8801235232189, 8801235225846, 8801235236002, 8801235226010)

### 납기요청일 기준 최근 미발주 상품 리스트업 (분석, 코드 변경 없음)
- 매칭된 50건의 납기요청일 중 최댓값(마지막 발주일)을 오늘(2026-08-06) 기준 경과일수로 계산해 정렬
- 90일 이상 발주 없는 13건 + 발주내역 전무 5건, 총 18건을 재고/발주 점검 대상으로 보고(코드 반영 없이 리스트만 전달, 기준일수는 추후 조정 가능)

## 2026-08-07

### 롯데알미늄 발주내역 매칭 실패 5건 수기 보완 완료 (data/db.json, 사용자 직접 입력)
- 전일 매칭 실패로 남겼던 5건(2020060400003, 8801235232189, 8801235225846, 8801235236002, 8801235226010)을 사용자가 더 과거 거래 내역을 직접 찾아 CTN바코드/판매처관리코드/판매처품목명 수기 입력 완료 — 이로써 롯데패키징솔루션즈(주) 생활용품 판매처 55건 전량 매칭 완료
- 그중 `2020060400003`(주방용트럼펫레이저헤드세트)은 판매처별 상태가 "단종"으로 다른 항목과 다른데, 사용자가 별도로 단종 여부를 확인함(정상 상태 값)

### 롯데패키징 상품 일괄 수정 (data/db.json, API 경유)
- 수급형태가 "무역(수입)"인 상품(51건)의 발주부서를 글로벌소싱부로, 롯데패키징 55건 전체의 영업담당자를 최찬희로 일괄 입력
- 롯데패키징 55건 전체의 판매처별 납품방법을 용차로 일괄 변경
- 90일 이상 발주없는 15건(2026-08-06 분석 당시 13건이었으나, 롯데알미늄 발주내역 파일이 그 사이 갱신되어 재계산 결과 15건으로 조정) 상품 메모에 "90일 이상 발주없음"을 기존 메모 뒤에 이어붙임

### "임가공" 필드 신설 — 판매처별(다중 태그) 필드로 설계 (server.js, public/app.js, public/styles.css)
- 처음에는 상품 단위 필드로 구현했으나, 사용자 피드백에 따라 임가공은 판매처마다 다르게 적용돼야 하는 정보라는 점을 반영해 상품 단위가 아닌 판매처 정보(`deliveryVendorInfo`) 안의 판매처별 필드로 재설계함 — 기본상품정보/판매현황 미등록상품/구매 품목 리스트 세 화면이 공유하는 "판매처 정보" 표(`setupVendorInfoTable`)에 물류대행 다음 컬럼으로 추가(쉼표구분 다중 태그, datalist 자동완성)
- 값 목록(바코드부착/소분/특수포장)은 기존과 동일하게 기본정보 > 기초코드 화면에서 추가/삭제 관리
- 리스트 화면의 "임가공" 컬럼도 상품 단위가 아닌 판매처별 행 데이터를 표시하도록 변경, 위치도 카테고리 옆에서 물류대행 옆(판매처별 속성 그룹)으로 이동
- 버그 수정: 처음 상품 단위 컬럼으로 추가했을 때, 두 상품 표(`#products-table`/`#sales-new-products-table`)가 `table-layout: fixed`로 전체 폭과 컬럼별 폭을 모두 픽셀로 고정해두는 구조라는 걸 몰라서 새 컬럼에 폭 규칙을 안 넣었더니 0px로 찌그러져 화면에 전혀 안 보이는 문제가 있었음(사용자 제보로 발견) — styles.css에 컬럼 폭(130px) 추가 및 두 표의 전체 고정폭도 그만큼 증량해 해결
- 사용자가 "미등록상품 수정창에 임가공이 안 보인다"고 제보 — 원인은 명확히 특정하지 못했으나(코드상으로는 상품 단위 입력칸이 존재했음), 이번 판매처별 필드 재설계로 대체되며 함께 해결됨

### "임가공" 입력 방식을 쉼표 텍스트 → 드롭다운+태그칩 다중선택으로 개선 (public/app.js, public/styles.css)
- 사용자 피드백: 쉼표로 구분해 직접 타이핑하는 방식은 다중선택이라 부르기 애매하고 실수하기 쉬움 — 드롭다운에서 골라 추가하면 태그 칩으로 쌓이고 칩의 x로 개별 삭제할 수 있는 방식(다른 다중선택 필드들과 동일한 UX)으로 교체
- 판매처 정보 표 안에서 컬럼 위치도 "물류대행 옆"에서 "맨 오른쪽(삭제 버튼 바로 앞)"으로 이동 — 출고적재사양 5칸 뒤에 위치

## 2026-08-10

### 롯데패키징 미등록상품 29건 리스트 엑셀 출력 (`scripts/export-lotte-packaging-unregistered-29.js`)
- 08-07에 조사해둔 롯데패키징 실재고 미등록 29건 리스트(자재명/물류바코드/실재수량)를 db.json 재대조(29건 전량 여전히 미등록 확인) 후 엑셀로 출력 — `자료/롯데패키징 미등록상품 29건.xlsx`
- 사용자가 "이 29건은 2년내 출고가 없었다는 뜻이냐"고 확인 요청 → movements가 productCode 기준으로 기록되는데 이 29건은 상품 등록 자체가 없어 이력이 붙을 대상이 없다는 점을 설명(WMS 이력 0건 ≠ 실제 출고 이력 0건, 다른 코드로 등록되어 있었을 가능성 있음)

## 2026-08-11

### 롯데패키징 이카운트 품목그룹 전수 대사 및 정리 (data/db.json, server.js, public/app.js)
- `자료/이카운트 품목그룹 롯데패키징.xlsx`(96건: 상품84·제품5·원자재3·부자재4)와 WMS 전체 대사 → 41건 완전 누락 확인, 미등록상품에 추가(구분값 원재료→원자재/부재료→부자재로 매핑)
- 41건에 "2년내 출고이력없음"(`_salesQty` 필드 없음으로 판별), 메모가 "90일 이상 발주없음"이던 14건은 메모를 지우고 `noRecentOrder` 불리언 필드로 전환 — 관리코드 옆 배지로 표시, 판매처별 상태는 41건 모두 "단종"으로 일괄 설정
- 구매처 영문명 66건은 수급형태=무역(수입)/발주부서=글로벌소싱부로, 롯데패키징 96건 전체는 영업담당자=최찬희/거래유형=내수/출고단위=INN/납품방법=용차로 통일
- 96건 전체를 미등록상품 → 기본상품정보로 이동(이동 과정에서 `noRecentOrder` 필드가 upsertProduct 화이트리스트에 없어 유실될 뻔한 걸 발견해 server.js에 추가) — 기본상품정보 452→548건
- 자동 삽입된 안내성 메모 94건 삭제(수기 작업메모 2건은 보존)
- 기본상품정보/판매현황 미등록상품 양쪽 탭에 "롯데패키징만" 임시 필터 버튼 추가 (작업 종료 후 제거 가능)

### BOM 편집창 기능 확장 (server.js, public/app.js, public/styles.css)
- BOM 미등록상품이 원자재코드 검색에서 기본상품정보만 조회되는 이유를 설명(server가 db.products에 없는 코드는 BOM 저장 시 필터링)
- 구성원자재 행에 품목명/규격/단위 자동표시 컬럼 추가
- "공정순서" 기능 신설 — 라우팅(공정별 시간/담당자/원가)까지는 만들지 않고, 1번부터 번호매겨지는 순서 리스트 + 자유 텍스트(자주 쓰는 키워드는 자동완성)로 설계(이유: 그 값을 소비하는 스케줄링/원가 기능이 없어서 구조화할 필요가 없다고 판단)
- BOM 대상 상품(완제품) 메모칸 신설 — 기존 note/memo 필드를 그대로 재사용
- 공정순서/구성원자재 모두 "고정값으로 보기 → + 추가/수정 버튼 눌러야 편집 가능" 방식으로 변경(버튼 문구도 "추가/수정"으로 변경), 저장하면 다시 고정값 보기로 전환
- 완제품 제목에 WMS코드/이카운트코드/SKU바코드를 라벨과 함께 2배 크게 표시
- 공정순서를 표 형태 대신 컴팩트한 인라인 칩 리스트로 변경
- 버그: 읽기전용 전환 시 입력창 폭을 글자수로 추정했더니 한글 폭 과소평가로 텍스트가 잘리는 문제 → 실제 렌더 폰트로 캔버스에서 측정하는 방식으로 교체, 모달이 display:none인 상태에서 폭을 계산하던 순서 문제도 함께 수정