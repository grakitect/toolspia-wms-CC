# 작업일지

작업 내용을 날짜별로 기록합니다. 새 작업은 파일 맨 아래에 이어서 추가합니다.

---

## 2026-07-07

### 바코드 인쇄 페이지 (public/barcode-print.html)

**버그 수정**
- 규격 컬럼 앞에 "0"이 붙어 나오던 오류 (재고값 0 고정 → 표시 로직 수정)
- 라벨 내부 여백 적용 시 바코드/글자가 잘리던 오류 (인쇄물 + 화면 미리보기)
- 미리보기에서 여백이 실제보다 과도하게 적용되던 오류 (축소 비율 미반영)
- 바코드 숫자 크기(%)를 키우면 글자가 잘려 깨지던 오류 (폰트 크기 상한 추가)
- 바코드 숫자 폰트 저해상도(203dpi 프린터) 대응 — 굵은 Arial 계열로 변경, 최소 크기 상향
- 체크박스 컬럼이 상품명과 겹쳐 보이던 오류
- 검색 후 전체선택 클릭 시 검색이 풀리던 오류
- 미리보기와 실제 인쇄물의 바코드 숫자 크기가 서로 다른 공식으로 계산되어 어긋나던 문제 — 인쇄용 계산값(mm)을 미리보기가 그대로 공유하도록 통일

**기능 추가**
- 상품 검색 테이블에 바코드(INN) / 바코드(CT) / 판매상태 / 판매처 컬럼 추가
- 판매처 드롭다운 필터 + 검색창에서 판매처명 검색 지원
- 롤지 라벨 사이즈 프리셋 버튼 (80×50, 100×50, 100×150 기본 등록, 직접 추가/삭제 가능, 브라우저에 저장)

**UI 정리**
- 컬럼 헤더 정비: 바코드→바코드(SKU), SKU→품목코드(이카운트), 재고→규격, 가격 컬럼 삭제
- 상품명/판매상태 등 컬럼 너비 재배분
- "바코드(중포)" 표기를 "바코드(INN)"으로 전체 통일 (public/app.js 포함)
- 인쇄설정/미리보기 레이아웃 비율 2:1로 조정, 미리보기 확대
- 상품 데이터베이스 툴바 정리: 엑셀업로드·양식다운로드·상품추가·CT/INN선택·카테고리선택 버튼 삭제 (판매처 드롭다운으로 대체)

### 기타
- 이마트·롯데마트 재고조사용 엑셀 리스트 생성 (`scripts/export-emart-lotte-stocktake.js`) — 판매처 중복 시 한 행에 함께 표시, 전산재고 포함

---

## 2026-07-10

### 개발 메모 (신규 메뉴)
- 사이드바 최하단에 "개발 메모" 메뉴 신규 추가 — 보드 > 중제목 > 할일 3단계 구조
- 할일을 독립된 카드(칸반 유닛)로 표시, 드래그 앤 드롭으로 같은/다른 중제목·보드 간 이동 및 순서 변경
- 할일 텍스트 자동 줄바꿈(textarea 자동 높이 조절)
- 체크박스·추가(+)/삭제(×) 버튼은 평소엔 숨겼다가 마우스 오버 시에만 표시
- 참고 이미지 기반으로 UI 여러 차례 다듬음 (아이콘 정렬, 버튼 크기/간격, 텍스트 라벨 등)

### 판매처별 코드/상품명 다중화
- 상품 데이터에 `deliveryVendorInfo`(판매처별 { 판매처, 코드, 상품명 }) 배열 도입
  - 기존 단일값 필드(`deliveryVendorCode`/`deliveryItemName`) 제거, 마이그레이션 추가 (판매처가 여러 개면 데이터가 실제로 속한 판매처에 정확히 배정 — 예: 이마트+롯데마트 상품은 기존 값을 롯데마트 항목으로 이관)
- 기본상품정보 / 구매품목리스트 목록을 판매처 수만큼 행으로 펼쳐서 표시, 같은 상품끼리 배경색으로 묶고 "판매처구분"(단일/다중) 컬럼 추가
- 상품 등록/수정 팝업의 판매처 입력을 판매처별 코드/상품명 테이블 방식으로 교체
- 엑셀 업로드(같은 품목코드 행을 그룹으로 묶어 판매처별 정보로 합침)·다운로드(행 펼치기 + 판매처구분 컬럼) 동일하게 반영
- "규격" 컬럼을 "품목명(이카운트)" 바로 옆으로 이동

### 판매처별 출고관리 - 마스터 탭 통일
- 이마트·다이소의 "마스터" 탭을 롯데마트와 동일한 방식으로 통일 — 코드마스터 업로드 대신 기본상품정보 기반 읽기 전용 목록으로 표시, "판매처 상품명" 컬럼 추가
- (참고: 기존 코드마스터 파일 업로드/삭제 UI는 화면에서 제거됨. 데이터/API 자체는 남아있어 출고 파싱 로직엔 영향 없음)

### 데이터 일괄 보정 (일회성 스크립트, `scripts/`)
- `backfill-emart-vendor-names.js`: 그동안 쌓인 이마트 출고 업로드 이력(6762건)에서 상품코드로 매칭해 판매처 코드/상품명 100건 백필
- `update-from-ecount-product-info.js`: `상품정보/이카운트 상품정보.xlsx` 기준으로 품목코드 매칭 후 품목명(상품명+규격 분리)/구분/규격 448건 전수 대조, 177건 수정 (BOM은 파일에 값이 없어 미반영)
- 두 스크립트 모두 적용 전 `data/db.backup-before-*.json` 백업 생성

---

## 2026-07-13

### 기본상품정보 화면 개선 (public/app.js, public/styles.css)
- 판매처 컬럼을 카테고리처럼 색상 태그(칩)로 복원 (`VENDOR_FIXED_COLORS`/`renderVendorChip`) — 판매처별 코드/상품명 다중화 작업 때 빠졌던 것 재적용
- 상품 검색 범위 확대: 품목코드/바코드(SKU·INN·CT)/품목명뿐 아니라 판매처관리코드·판매처 품목명·구매처·구매처 품목코드까지 검색되도록 인덱스 확장
- 품목코드(이카운트) 열을 좌측 고정(sticky) 처리하고, 오른쪽에 스크롤 시에만 나타나는 그림자 효과 추가
  - 시행착오: box-shadow(outset)가 `border-collapse: collapse` 테이블에서 옆 셀에 가려 안 보이는 문제 발견 → `::after` 가상요소로 스크롤 콘텐츠 쪽에 그림자를 얹는 방식으로 해결
  - 최종값: 폭 30px, 시작 투명도 0.12, 스크롤 시에만 표시(`is-scrolled-x` 클래스 토글)
- 컬럼 고정 로직(`applyTableColumnConfig`/`openColumnSettingsUI`)을 1개→N개 고정 컬럼 지원하도록 일반화, 예전에 저장된 컬럼 순서 설정이 있어도 고정 열이 항상 맨 앞에 오도록 버그 수정
- "판매중만 표시" 체크박스 기본값 반전: 기본 상태에서 판매중 상품만 표시, 체크 시 "단종품 포함"으로 전체 표시

### 적재수량/영업담당자 필드 추가 (server.js, public/app.js)
- 신규 컬럼 6개: CTN(EA), INN/CTN, INN(EA), CTN/PLT, PLT(EA), 영업담당자 — 목록/등록폼/엑셀 업로드·다운로드·가이드 전체 반영
- "바코드(CT)" 라벨을 "바코드(CTN)"으로 전체 화면(기본상품정보·구매품목리스트) 통일
- 버그 수정: `upsertProduct`가 필드 화이트리스트 방식이라 새 필드가 저장 시 조용히 삭제되던 문제 발견 → 화이트리스트에 추가, 서버 재시작 후 API 라운드트립으로 검증

### 이마트 판매처 데이터 정리 (scripts/)
- `update-emart-vendor-names-20260713.js`: "이마트 상품정보 판매처상품명 정리-2026-07-13-1.xlsx" 기준 판매처 품목명 206건 매칭, 96건 실제 변경 (매칭 키를 상품코드가 아닌 이카운트코드 우선으로 수정해 미매칭 171건→0건 해결)
- `fill-emart-vendor-code-from-barcode.js`: 이마트 판매처관리코드를 바코드(SKU)와 동일하게 채움 (100건)
- `fill-emart-ct-from-inn.js`: 이마트 판매 상품의 바코드(CT)를 바코드(INN)과 동일하게 채움 (186건)
- 각 스크립트 적용 전 `data/db.backup-before-*.json` 자동 백업

---

## 2026-07-14

### 이마트 확정리스트 유실 복구 및 자동삭제 버그 근본 수정 (server.js)
- 이마트 판매입력 확정리스트가 통째로 사라진 것을 발견 → 백업(`data/db-backups/`)에서 복구 가능한 이마트/롯데마트 확정리스트 전부 복구
- 근본 원인 파악: `outbound-order-upload`의 `apply-batch`/`delete-batches`가 `slipWorkflow`(전 판매처 공용 flat map) 전체 키를 순회하며 지우는 바람에, 한 판매처 파일을 재적용/삭제할 때 다른 판매처의 `slipWorkflow`·`confirmedLists`가 같이 삭제되던 구조적 버그
- 수정: 삭제 대상을 현재 판매처의 slip key로만 한정(`oldPartnerSlipKeys` 캡처), `confirmedLists` 자동 초기화 로직 완전 제거 → 확정리스트는 이제 어떤 업로드/재적용 액션으로도 사라지지 않음
- 실제 장애 사례(여주DRY센터 확정리스트의 판매입력 전송 막힘)를 이 버그로 근본 원인 규명하고 안전하게 패치(재고 중복 반영 없이 `slipWorkflow` 상태만 보정)

### 구매품목리스트 개선 (public/app.js)
- 기본상품정보에 있던 적재수량 컬럼(CTN(EA)/INN·CTN/INN(EA)/CTN·PLT/PLT(EA)) 동일 반영
- "단종품 포함" 토글 추가(기본상품정보와 동일한 패턴)
- 판매처 컬럼에 색상 태그(칩) 적용

### 전체 검색 기능 통합 확장 (public/app.js, server.js)
- 공용 함수 `buildProductSearchText(p)` 신설 — 품목코드/바코드 3종/품목명/판매처코드·상품명/구매처/구매처품목코드/규격까지 한번에 검색
- 마스터·재고·위치재고·조정/이동 폼·바코드 인쇄 검색 등 WMS 내 모든 상품 검색창에 동일 적용
- `/api/history`(입출고 이력) 검색 범위도 동일 수준으로 확장

### 바코드 인쇄 2 신규 구축 (public/barcode-print2.html, 신규 파일)
- BoxHero(boxhero.io/print) 스타일을 참고해 기존 별도 프로그램이던 바코드 인쇄를 WMS 통합형으로 재설계 — 커스텀 라벨 편집기는 유지
- 용지설정(A4라벨용지/롤라벨용지/사용자정의) → 라벨 디자인(기본/커스텀 템플릿 캐러셀) → 상품검색(WMS 상품 담기) → 데이터입력(미등록 상품 수동 입력) 4단계 구성, 미리보기 패널 1000px 고정
- 인쇄/PDF 저장 버튼을 미리보기 타이틀 라인으로 배치, "+커스텀 템플릿 추가"는 점선 버튼 스타일로 라벨 템플릿 페이지와 연결
- 커스텀 템플릿을 서버(db.json, `barcodeCustomTemplates`)에 저장해 바코드 인쇄 1/2가 공유하도록 변경
- 폼텍(Formtec) 라벨지 프리셋 데이터 보강 — 기존 15종 + 신규(비충돌) 27종 추가
- 버그 수정: A4 시트 모드 미리보기/실제 인쇄 크기 불일치(마진 비대칭 케이스), "기본 바코드" 템플릿에서 상품명 텍스트 하단 잘림
- 이마트 판매입력 확정리스트에 "확정 시간" 컬럼 추가

### "자동/무성 삭제" 버그 클래스 재감사 및 수정
- 사용자 피드백으로 범위 재정의: UI 삭제 버튼의 confirm() 여부가 아니라, "실행 버튼을 누르지 않았는데 다른 작업의 부작용으로 자동으로 지워지는 것"이 실제 문제였음(위 확정리스트 버그와 동일 클래스)
- `server.js` 전수 감사로 2건 확정:
  1. **상품 엑셀 업데이트(`/api/products/bulk`) 전체 필드 덮어쓰기**: 일부 컬럼만 있는 업데이트용 시트를 올리면 시트에 없는 필드(안전재고/구매처/카테고리/판매처/비고 등)가 조용히 빈 값으로 초기화되던 문제 → 업로드 시트에 실제로 존재하는 컬럼만 `presentFields`로 표시해 서버에 전달, 없는 필드는 기존 값 유지하도록 `upsertProduct` 수정(단건 등록/수정 폼은 기존처럼 전체 덮어쓰기 유지). 격리 테스트로 3가지 시나리오(부분 업로드/전체 업로드/수동 폼) 모두 검증
  2. **바코드 커스텀 템플릿 동시저장 충돌**: 두 탭/사용자가 동시에 템플릿을 저장하면 전체 배열 덮어쓰기 방식 때문에 한쪽 저장이 다른 쪽이 방금 추가한 템플릿을 지우는 구조 → `/api/barcode-custom-templates`를 항목 단위 `/upsert`, `/delete` API로 교체(barcode-print.html/2.html 동일 반영), 실제 동시 저장 시나리오로 데이터 유실 없음을 확인
- kanban(개발 메모) 카드 삭제, 커스텀 템플릿 삭제 버튼에 `confirm()` 확인창 추가(사용자 요청으로 그대로 유지)

### UI 다듬기
- 롤라벨용지 "자주 쓰는 사이즈" 프리셋을 알약 모양 → 각진 버튼으로 변경, 회색 톤 스타일 적용
- 바코드 인쇄 1/2 새로고침 시 화면이 비어 보이던 버그 수정 — 두 화면은 iframe을 클릭 시에만 지연 생성하는데, 새로고침 후 마지막 화면 복원(`switchView`) 로직에는 그 생성 호출이 빠져 있었음
- 바코드 인쇄 2의 "라벨 디자인" 단계에서 커스텀 템플릿 캐러셀 위에 중복으로 남아있던 "커스텀 템플릿" 라벨 텍스트 제거
